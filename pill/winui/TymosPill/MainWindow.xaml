<Window
    x:Class="TymosPill.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Tymos"
    ExtendsContentIntoTitleBar="True">

  <Grid x:Name="Root" Background="Transparent">
    <Border
        x:Name="PillChrome"
        HorizontalAlignment="Center"
        VerticalAlignment="Bottom"
        Margin="0,0,0,36"
        Padding="14,10,18,10"
        CornerRadius="999"
        Background="{StaticResource PillBg}"
        BorderBrush="{StaticResource PillBorder}"
        BorderThickness="1"
        PointerPressed="Pill_PointerPressed"
        PointerMoved="Pill_PointerMoved"
        PointerReleased="Pill_PointerReleased"
        PointerCaptureLost="Pill_PointerCaptureLost">

      <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
        <Ellipse
            x:Name="StatusDot"
            Width="8"
            Height="8"
            Fill="{StaticResource PillAccent}"
            VerticalAlignment="Center" />

        <TextBlock
            x:Name="TimeText"
            Text="24:32"
            FontFamily="Cascadia Mono, Consolas"
            FontSize="18"
            FontWeight="Normal"
            Foreground="{StaticResource PillText}"
            VerticalAlignment="Center" />

        <Rectangle
            Width="1"
            Height="18"
            Fill="{StaticResource PillDivider}"
            VerticalAlignment="Center" />

        <TextBlock
            x:Name="TitleText"
            Text="Ship floating pill"
            FontFamily="Segoe UI Variable, Segoe UI"
            FontSize="13"
            FontWeight="SemiBold"
            Foreground="{StaticResource PillText2}"
            MaxWidth="220"
            TextTrimming="CharacterEllipsis"
            TextWrapping="NoWrap"
            VerticalAlignment="Center" />

        <TextBlock
            x:Name="PhaseText"
            Text="FOCUS"
            FontFamily="Cascadia Mono, Consolas"
            FontSize="9"
            CharacterSpacing="140"
            Foreground="{StaticResource PillMuted}"
            VerticalAlignment="Center" />
      </StackPanel>
    </Border>
  </Grid>
</Window>
